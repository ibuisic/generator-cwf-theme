{#
/**
 * @file
 * Theme override to display a region.
 *
 * Available variables:
 * - content: The content for this region, typically blocks.
 * - attributes: HTML attributes for the region div.
 * - region: The name of the region variable as defined in the theme's
 *   .info.yml file.
 *
 * @see template_preprocess_region()
 */
#}
{%
  set classes = [
    'region',
    'collapse',
    'navbar-collapse',
    theme_settings.navbar_collapsed_classes ? theme_settings.navbar_collapsed_classes,
    'region-' ~ region|clean_class,
  ]
%}

{% set navbar_collapsed_container = theme_settings.navbar_collapsed_container|clean_class %}

{% if content %}
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarCollapsedRegion" aria-controls="navbarCollapsedRegion" aria-expanded="false" aria-label="Toggle navbar collapsed region">
    <span class="navbar-toggler-icon"></span>
  </button>
  <section{{ attributes.addClass(classes).setAttribute('id', 'navbarCollapsedRegion') }}>
    {% if (theme_settings.navbar_collapsed_container != 'no') %}
      <div class="{{ navbar_collapsed_container }}">
    {% endif %}
      {{ content }}
    {% if (theme_settings.navbar_collapsed_container != 'no') %}
      </div>
    {% endif %}
  </section>
{% endif %}